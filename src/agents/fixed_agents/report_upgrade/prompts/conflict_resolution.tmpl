以下数据存在冲突，请裁决：
指标：${metric}
冲突条目：
${conflict_entries}

补充搜索结果：
${search_results}

## 裁决要求

### 值裁决
1. 优先采用官方统计/权威研究机构的数据
2. 优先采用更新日期更近的数据
3. 给出裁决理由

### 口径裁决（必须逐项填写）
每个冲突条目包含以下口径字段，裁决时必须明确选择或合并：
- **geographic_scope**（地理口径）：中国/全球/其他——选择与报告主题匹配的口径
- **period**（时间口径）：2025年/2025年Q1/2026年预测等——选择最相关的时间范围
- **population**（统计对象）：出货量/激活量/销售额/ASP等——明确统计口径
- **epistemic_level**（认知层级）：factual（已确认）/ inferential（推断）/ speculative（推测）

### 裁决逻辑
1. 当冲突源于口径不同（如"全球出货量"vs"中国出货量"），保留与报告主题匹配的口径，标注另一个口径为参考
2. 当冲突源于时间不同（如2025实际 vs 2026预测），优先采用实际数据，预测数据标注为 speculative
3. 当冲突源于统计对象不同（如出货量 vs 激活量），明确区分并标注各自含义

输出JSON：{"canonical_value": "...", "canonical_unit": "...", "canonical_source": "...", "canonical_geographic_scope": "...", "canonical_period": "...", "canonical_population": "...", "canonical_epistemic_level": "...", "reason": "..."}
