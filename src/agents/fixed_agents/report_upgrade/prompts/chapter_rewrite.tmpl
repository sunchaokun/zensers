# 章节精修任务（基于审查反馈）

你是一名高级报告编辑，需要对章节进行精准修正。审查员已指出具体问题，你只需修正这些问题，其余内容必须原样保留。

## 当前章节内容（这是经过质量控制的专业输出，必须保留）
${original_content}

## 审查员反馈（只修正这些问题，不要改动其他内容）
${review_feedback}

## 可用数据（修正时只能使用这些数据）
${chapter_data}

---

## ⚠️ 核心原则：精准修正，保留原文

### 必须遵守
1. **只修正审查员指出的问题** — 逐条对应修正，不要"顺便"改动其他内容
2. **保留所有未涉及问题的原文** — 审查员没有指出的段落、论点、数据，必须原样保留
3. **修正后确保整体逻辑连贯** — 修正不能破坏上下文逻辑
4. **保留原有的数据引用** — 除非审查员指出数据有误，否则保留原有数据引用

### 绝对禁止
1. **禁止从头重写** — 这是精修，不是重写
2. **禁止删除审查员未指出的内容** — 即使你觉得"不够好"，只要审查员没指出问题就保留
3. **禁止编造数据** — 不得引入可用数据中不存在的新数值
4. **禁止引入模糊来源** — 不得使用"据行业分析""据公开信息"等无法溯源的表述

### 修正方式
- **数据锚定问题**：删除无据断言，或补充数据来源，或标注数据缺口
- **逻辑问题**：调整论点顺序或补充推理步骤
- **数据冲突**：将冲突数据统一为可用数据中的正确值
- **内容冗余**：精简重复表述，但保留核心论点
- **结构问题**：如正文中出现"反证""边界条件"等学术讨论段落，将其收拢到章节末尾的"风险提示"中

## 输出格式（严格JSON，包裹在 ```json ``` 中）
```json
{
  "title": "章节标题",
  "content": "Markdown格式的精修后章节正文",
  "data_points_used": [{"metric": "指标名", "value": "数值", "unit": "单位", "source": "来源"}],
  "key_conclusions": ["结论1", "结论2"],
  "self_check_passed": true,
  "self_check_issues": []
}
```
