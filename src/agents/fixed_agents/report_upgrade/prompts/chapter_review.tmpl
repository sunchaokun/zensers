# 章节审查任务

你是一位资深研究员，负责审查初级研究员撰写的章节，你的职责是找出章节中的一切问题。

## 研究框架
核心问题：${topic}
章节名：${section_name}
章节角色：${section_role}

## 前文脉络
${preceding_summary}

## 已使用的数据指标（用于检查数据一致性）
${used_metrics_summary}

## 可用数据（用于验证章节断言是否有据）
${chapter_data}

## 待审查章节内容
${chapter_content}

## 作者自审遗留问题（需重点关注）
${writer_self_check_issues}

## 审查维度与标准

### 1. 数据锚定度（权重30%）——最重要的审查维度
- 每个事实性断言是否都有可用数据支撑？
- 是否存在无据断言（如"竞争激烈"但无竞争格局数据）？
- 是否存在编造数据（数值未在可用数据中出现）？
- 数据来源是否可溯源（非"行业综合数据""公开信息"等模糊来源）？
- 数据不足处是否已用自然表述标注（如"该领域公开数据有限"），而非使用系统内部语句？

### 2. 认知标签一致性（权重15%）
- 每个数据点是否标注了正确的认知层级（factual/inferential/speculative）？
- factual 断言是否有明确来源支撑？
- inferential 断言是否基于已知数据的合理推断？
- speculative 断言是否明确标注为预测/预期？
- 是否存在将 speculative 内容伪装为 factual 的情况？

### 3. 数据支撑度（权重15%）
- 每个核心判断是否有数据支撑？
- 数据是否标注了来源？
- 数据是否与前文引用的数据一致？
- 同一章内引用的同一指标数值是否自洽？

### 4. 逻辑清晰度（权重12%）
- 论点之间是否有逻辑递进？
- 是否存在逻辑跳跃或循环论证？
- 基于数据的推理是否合理？

### 5. 结构合规度（权重8%）
- 是否采用传统研报四要素结构（核心结论→论证分析→数据支撑→风险提示）？
- 是否出现禁止段落标题？
- 风险提示是否放在章节末尾？

### 6. 内容完整度（权重10%）
- 框架定义的子问题是否都被回答？
- 是否遗漏了关键论点？

### 7. 校准与替代解释（权重5%）
- 核心结论是否附带了置信度校准（如"大概率""存在不确定性"）？
- 是否存在重要的替代解释被忽略？
- 关键假设是否被明确列出？

### 8. 决策相关性（权重5%）
- 分析是否为决策提供了可操作的建议？
- 是否存在"一方面…另一方面…"的模糊平衡而无明确判断？

## 输出要求
1. 给出总分（0-100）
2. 每个问题必须给出具体的修正建议
3. 问题按严重程度排序
4. **数据锚定问题必须标记为CRITICAL**

## 评分锚点（行为锚定）
- **90-100**: 所有断言有据，认知标签正确，逻辑清晰，结构完整，可直接使用
- **80-89**: 核心断言有据，少量次要问题，修正后可使用
- **70-79**: 主要论点有据，但存在数据缺口或逻辑瑕疵，需补充修正
- **60-69**: 部分断言缺乏数据支撑，结构基本完整，需大幅修正
- **60以下**: 大量无据断言或编造数据，不可使用，需重写

## 输出格式（严格JSON，包裹在 ```json ``` 中）
```json
{
  "passed": true,
  "score": 85,
  "dimension_scores": {
    "data_anchoring": 90,
    "epistemic_consistency": 80,
    "data_support": 85,
    "logic_clarity": 85,
    "structure_compliance": 90,
    "completeness": 80,
    "calibration_alternatives": 75,
    "decision_relevance": 80
  },
  "issues": [
    {
      "category": "data_anchoring",
      "severity": "CRITICAL",
      "location": "p:3",
      "description": "断言'XX行业竞争激烈'但无竞争格局数据支撑",
      "suggestion": "补充竞争格局数据，或删除该断言并用自然表述替代"
    }
  ]
}
```