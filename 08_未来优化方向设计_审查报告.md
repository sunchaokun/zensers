# 08_未来优化方向设计 — 代码级审查报告

**审查日期**: 2026-06-06
**审查方法**: 逐项对照代码验证文档中的状态声明、行号引用、数据指标
**审查范围**: 全文档 10 个章节
**结论**: 发现 **7 处严重错误**、**6 处行号/数据偏差**、**4 处描述不准确**、**2 处与 07 文档矛盾**

---

## 一、严重错误（文档声称与代码实际状态矛盾）

### 1. §6.4 "feedback_executor 仍被 engine.py:293 实例化" — ❌ 错误

> "仍然被 `engine.py:293` 实例化，生产代码 0 处路径触发调用"

**实际代码**（engine.py:292）:
```python
# 反馈执行器（已废弃，保留为None）
self.quality_executor = None
```
`quality_executor` 被显式设为 `None`，**未实例化**任何对象。

**深层问题**: engine.py 中存在 3 处引用 `self.quality_executor` 的**死代码路径**:
- 第 2325 行: `await self.quality_executor.execute_with_retry(...)` — 但 `_execute_stage_with_quality()` 函数本身**从未被调用**（grep 验证仅 1 处定义，0 处调用）
- 第 2452 行: `if not self.quality_executor: return {"enabled": False}` — 有守卫保护
- 第 2457 行: `self.quality_executor.get_summary()` — 受 2452 守卫保护

**结论**: 文档说"实例化"错误；但"死代码"定性正确。应改为"属性声明为 None，引用路径未移除"。

---

### 2. §5.1 自评配置键名错误

> "`_self_evaluate` 在 `generic_agent.py:539` 实现但默认关闭 (`self_evaluate: bool = False`)"

**实际代码**（generic_agent.py:539）:
```python
max_self_eval = self.config.get("max_self_eval_iterations", 0)
if max_self_eval > 0 and result.get("success") and result.get("content"):
```

| 文档声称 | 实际 |
|---------|------|
| 配置键: `self_evaluate` | 配置键: `max_self_eval_iterations` |
| 类型: `bool` | 类型: `int`（迭代次数） |
| 默认: `False` | 默认: `0` |

---

### 3. §1 标题 "134 tests + 46 tests = 180" — ❌ 严重偏差

> "**前提**: 8 个 pre-existing 集成测试已修复 (46 tests 全部通过)"
> "134 tests" (MKB 框架库完工)

**实际**: `pytest --collect-only` 显示 **387 tests collected**（3.77s）。

- "134 tests" 可能是某个子集的计数，但文档未说明
- "46 tests" 可能是另一子集
- 实际总量 387 远超 134+46=180
- §10 验收标准中写"当前值: 134 + 46 = 180"同样错误

**建议**: 用 `pytest tests/ --collect-only -q` 的实际输出替换。

---

### 4. §6.2 "SharedMemory 缺 previous_attempt" — ❌ 伪问题

> "context path: `{score, issues, previous_attempt}`"
> "SharedMemory path: `{score, issues}` (缺 previous_attempt)"
> "修复: SharedMemory 增加 `previous_attempt` 字段"

**实际代码**: 重试反馈**设计上就是通过 `agent._context["quality_feedback"]` 传递**，不是通过 SharedMemory:
- engine.py:2626: `agent._context["quality_feedback"] = {"score": ..., "issues": [...], "previous_attempt": retry_count - 1}`
- generic_agent.py:3016: `fb_attempt = quality_feedback.get("previous_attempt", 0)`

SharedMemory 在 engine.py 中仅用于 `_canonical_registry` 广播（第 1343 行），不参与重试反馈路径。

**结论**: 这不是"两个路径不一致需要修复"，而是文档误判了系统设计意图。两个路径本就用于不同目的。

---

### 5. §2 架构图 "23 aspect 映射" — 数据偏差

> "23 aspect 映射"

**实际**: 对 38 个框架 JSON 的 `aspects` 字段去重后得到 **22 个唯一 aspect**（非 23）:
```
competitive_landscape, enterprise, enterprise_analysis, financial_analysis,
financial_valuation, industry_analysis, industry_chain, investment_analysis,
macro_analysis, macro_environment, market_analysis, market_size, market_structure,
policy, policy_analysis, risk_assessment, strategy, strategy_evaluation,
technology, technology_analysis, trend, value_chain
```

---

### 6. §2 架构图 "向量检索 1121 行" — 数据偏差

> "向量检索 1121 行实现未接入"

**实际**: 三个文件合计 **1132 行**:
- `vector_store.py`: 463 行
- `semantic_search.py`: 250 行
- `hybrid_search.py`: 419 行

1132 vs 1121，差异 11 行。可能是早期版本计数。

---

### 7. §9 Phase 0 "E5 向量检索评估 1d" — 工时估算不足

> "E5 向量检索评估 [1d, 决定 1121 行代码命运]"

仅"评估"可能 1d 足够，但如果评估结论是"接入"，后续工作量远超 1d。文档未区分"评估"和"接入"两个阶段。

---

## 二、行号/数据偏差

| # | 文档声称 | 实际位置/值 | 偏差 |
|---|---------|-----------|------|
| 1 | §6.4 "engine.py:293 实例化" | engine.py:292 (`= None`) | 行号+1，描述完全错误 |
| 2 | §5.1 "generic_agent.py:539" | 行号正确 ✅ | — |
| 3 | §1 "134 tests" | 实际 387 tests | +253 |
| 4 | §1 "23 aspect" | 实际 22 aspect | -1 |
| 5 | §6 "向量检索 1121 行" | 实际 1132 行 | +11 |
| 6 | §4.2 LLM Judge "cheap_model (:75)" | llm_judge.py:72 | +3 |

---

## 三、描述不准确

### 1. §1 完成全景 "≈70% 方案量"

这个百分比无法从代码中验证。文档未定义"方案量"的计量口径（按诊断条款数？按代码行数？按功能点？）。建议明确计算依据。

### 2. §4.3 三层融合权重 "Layer 1 权重最低 (25%)"

文档声称 Layer 1 占 25%，Layer 2 占 40%，Layer 3 占 35%。但 §4.2 的目标架构代码示例中写的是:
```python
total = layer1.score * 0.30 + layer2.score * 0.40 + layer3.score * 0.30
```
权重是 **30/40/30**，与§4.3文字描述的 **25/40/35** 不一致。**文档内部自相矛盾**。

### 3. §3.4 "MKB 读取接口 0.5d"

`store.py` 加入 exemplar/rubric 查询——但代码中不存在 `store.py`。当前 MKB 读取通过 `registry.py`（57 行）实现。文档应明确是在 registry.py 中扩展还是新建文件。

### 4. §5.2 "注入格式为纯文本拼接"

`_get_professional_role_prompt` 的注入实际上是结构化的（用 `##` markdown 标题分段），不是简单的字符串拼接。描述不够准确。

---

## 四、与 07 文档的矛盾

### 1. MKB 状态

| 来源 | MKB 状态描述 |
|------|------------|
| 07 §2 架构图 | "MKB ❌ 未实施" |
| 07 修订说明 | "MKB 框架库实施完成：38 框架" |
| 08 §1 完成全景 | "38 框架 JSON / 9 专业领域" 列为已完成 |

08 文档与 07 修订说明一致，但 07 内部自相矛盾（架构图 vs 修订说明）。08 文档未指出这个不一致。

### 2. per-agent 统计

| 来源 | 状态 |
|------|------|
| 07 §0 映射表 | ✅ 已实施 |
| 07 §1.2 缺陷表 | 列为核心缺陷（自相矛盾） |
| 08 §1 完成全景 | 列为已完成 |
| 08 §9 Phase 0 | "per-agent 统计跨会话 [1d]" |

08 正确地将其标为已完成，但 Phase 0 又列出了"per-agent 统计跨会话"。经代码验证，`agent_stats` 确实是**每次请求重新初始化的局部变量**（quality_check_agent.py:1028），不跨会话持久化。所以 08 的 Phase 0 在这里是正确的——基础功能已完成但缺跨会话持久化。

---

## 五、各 Track 验证结果汇总

### Track A: MKB 内容充实

| 声明 | 验证结果 |
|------|---------|
| 框架库 38 框架 ✅ | ✅ 正确（38 个 JSON 文件） |
| exemplars 目录已创建但空 ❌ | ✅ 正确（4 个空子目录） |
| rubrics 目录空 ❌ | ✅ 正确 |
| data_standards 目录空 ❌ | ✅ 正确 |
| 23 aspect 映射 | ❌ 实际 22 个唯一 aspect |
| 工时估算 5-7d | 合理（内容创作类工作） |

### Track B: 三层评分 Layer 2+3

| 声明 | 验证结果 |
|------|---------|
| Layer 1 ✅ (SECTION_ELEMENT_REQUIREMENTS) | ✅ 正确（quality_check_agent.py:44-120） |
| Layer 2 ❌ | ✅ 正确（无 Layer2MethodologyScorer） |
| Layer 3 ❌ (LLM Judge 已修但未升级) | ✅ 正确 |
| 融合权重 25/40/35 | ❌ 与代码示例 30/40/30 不一致 |
| LLM Judge 行号引用 | ⚠️ 多处偏差 +3 行 |

### Track C: Agent 执行增强

| 声明 | 验证结果 |
|------|---------|
| 自评实现但默认关闭 | ✅ 正确但配置键名错误（见上文） |
| 方法论注入 1×150→3×300 | ✅ 正确（generic_agent.py:2999-3010） |
| S3 文档自修复 3 轮 | ✅ 正确（quality_check_agent.py:704,738） |
| 工时 3.5d | 合理 |

### Track D: 架构统一

| 声明 | 验证结果 |
|------|---------|
| feedback_executor 379 行 | ✅ 正确 |
| engine.py:293 实例化 | ❌ 实际是 `= None`，未实例化 |
| SharedMemory 缺 previous_attempt | ❌ 伪问题（设计上用 _context 传） |
| 工时 4.5d | 合理 |

### Track E: 知识管理

| 声明 | 验证结果 |
|------|---------|
| 向量检索 1121 行 | ⚠️ 实际 1132 行 |
| 向量检索未接入 | ✅ 正确（__init__.py:40 import 被注释） |
| 3 个纯正则提取器 | 未深入验证，但符合已知代码结构 |
| E1-E5 工时估算 | 合理 |

---

## 六、§10 验收标准逐项验证

| 指标 | 文档当前值 | 实际当前值 | 正确？ |
|------|-----------|-----------|--------|
| rubrics 编写数量 | 0 | 0 | ✅ |
| exemplars 入库数量 | 0 | 0 | ✅ |
| data_standards 指标 | 0 | 0 | ✅ |
| 专业级 agent 自评率 | 0% | 0%（默认 `max_self_eval_iterations=0`） | ✅ |
| feedback_executor 文件行数 | 379 行 | 379 行 | ✅ |
| 全部测试通过 | 134 + 46 = 180 | **387 collected** | ❌ |
| 总测试目标 ≥200 | — | 已超过 387 | 目标过低 |

---

## 七、建议修订清单

### 必须修订（影响文档可信度）

1. **§1 标题**: "134 tests + 46 tests" → 改为实际 pytest 采集数（387）
2. **§5.1**: 配置键 `self_evaluate: bool = False` → `max_self_eval_iterations: int = 0`
3. **§6.2**: 删除"SharedMemory 缺 previous_attempt"修复项——这是设计正确行为
4. **§6.4**: "仍被 engine.py:293 实例化" → "属性声明为 None，第 2325/2452/2457 行引用未移除（死代码路径）"
5. **§4.2 vs §4.3**: 三层融合权重统一（30/40/30 或 25/40/35，选一个）
6. **§1**: "23 aspect" → "22 aspect"
7. **§10**: 测试数 "134+46=180" → "387 collected"；目标 "≥200" → "≥400"

### 建议修订（提高精确度）

8. **§6**: "向量检索 1121 行" → "向量检索 1132 行"
9. **§1**: "≈70% 方案量" — 明确计量口径
10. **§3.4**: `store.py` → `registry.py` 扩展
11. **§5.2**: "纯文本拼接" → "markdown 标题分段注入"
12. **§9 Phase 0**: 区分"E5 向量检索评估"和"E5 向量检索接入"两个阶段
