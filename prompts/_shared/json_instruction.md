## JSON Output Requirements
1. Output only valid JSON, do not include markdown code block markers
2. Do not include comments, explanations, or other non-JSON text
3. Ensure all strings use double quotes
4. Do not add trailing commas

When using code blocks, use the following format:
```json
{
    "key": "value"
}
```
